{
  "name": "backend-api-server",
  "version": "2.0.0",
  "description": "Server API module with gulp 4.0",
  "main": "index.js",
  "author": "IBM",
  "license": "MIT",
  "scripts": {
    "_comment": "mkdir -p dist && cp -R config server index.js  dist/",
    "dist": "mkdir dist && xcopy /E config dist && xcopy /E server dist && xcopy /E index.js dist",
    "start": "node -r esm index.js"
  },
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "dateformat": "^3.0.3",
    "dotenv": "^8.2.0",
    "esm": "^3.2.25",
    "express": "^4.17.1",
    "mongoose": "^5.8.5",
    "uuidv4": "^6.0.0"
  }
}
